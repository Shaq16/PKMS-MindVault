<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/activity_capture_root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_dark"
    android:padding="24dp">


    <TextView
        android:id="@+id/titleCapture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:text="Add Your Thoughts"
        android:textColor="#1C1C22"
        android:textSize="26sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.496"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/inputThought"
        android:layout_width="0dp"
        android:layout_height="180dp"
        android:background="@drawable/rounded_input_light"
        android:hint="Write what's on your mind..."
        android:textColor="#FFFFFF"
        android:textColorHint="#A999FF"
        android:padding="16dp"
        android:gravity="top|start"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/titleCapture"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/saveBtn"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:text="Save Thought"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="#7A5CFA"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/inputThought"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
